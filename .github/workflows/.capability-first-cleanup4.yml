name: Final audit wording fix

on:
  push:
    branches:
      - agent/capability-first-cleanup

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/capability-first-cleanup
          fetch-depth: 0
      - run: python3 scripts/.capability_first_cleanup4.py
      - run: |
          git config user.name "starintel-auto-research[bot]"
          git config user.email "starintel-auto-research[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No final wording changes were produced"
            exit 1
          fi
          git commit -m "Remove final mandatory audit wording"
          git push origin HEAD:agent/capability-first-cleanup
