# Starintel Claude Code Memory

@AGENTS.md

Use `AGENTS.md` as canonical policy. Load only the active implementation design for the selected project and its direct dependencies. Use `skills/*/SKILL.md` for procedures. Run `scripts/sync.py --check` before completion.
