:PROPERTIES:
:ID:       star-kb-research-004-portable-rule-reuse-across-logic-actors
:END:
#+title: STAR-KB-RESEARCH-004 Portable Rule Reuse Across Logic Actors
#+description: Approved research on making star-kb reusable by prolog-query-server, direct Prolog actors, and StarLang logic actors, including portable semantics, trust labels, sandbox-required logic, actor deltas, backend parity, and runtime-selection evidence.
#+status: REVIEW
#+approval_schema: adard.research-approval.v1
#+approval_state: APPROVED
#+approval_actor: nsaspy / StarIntel operator
#+approval_evidence: Operator explicitly approved STAR-KB-RESEARCH-004 in ChatGPT on 2026-09-10 and instructed that it move to design.
#+approval_base_commit: 448f313bc75c0dcd95bdcaedc98b97b3ae53ab7b
#+approval_base_blob: 15da0134ef3ffea7832c682307506eecb5b68a0b
#+approval_decided_at: 2026-09-10
#+created: [2026-09-10 Thu]
#+last_modified: [2026-09-10 Thu]
#+filetags: :starintel:star-kb:prolog:star-lang:logic:actors:rules:reuse:portable:sandbox:safety:research:aradr:approval:
#+todo: TODO RESEARCHING REVIEW BLOCKED | DONE REJECTED

* Approval Table

| Approval area | Required authority | State | Evidence required | Evidence reference |
|---------------+--------------------+-------+-------------------+--------------------|
| Portable rule-reuse research conclusion | StarIntel operator | APPROVED | Explicit human approval of this research conclusion | Operator approval in ChatGPT on 2026-09-10 |
| Logic-package design | StarIntel operator | PENDING | Approved research plus explicit request to begin design, followed by design review | [[id:star-kb-design-002-portable-logic-packages-sandboxed-actors][STAR-KB-002 Portable Logic Packages and Sandboxed Actors]] |
| Implementation | Relevant repository maintainers | NOT STARTED | Explicitly approved governing design plus implementation evidence | No implementation authorization |

* Changelog

| Date | Change | Author or actor | Evidence |
|------+--------+-----------------+----------|
| 2026-09-10 | Record explicit operator approval of this research conclusion and the instruction to advance it to design; link the resulting STAR-KB-002 design while leaving implementation unstarted. | ChatGPT ARADR pass | Operator approval/instruction in ChatGPT; [[id:star-kb-design-002-portable-logic-packages-sandboxed-actors][STAR-KB-002]] |
| 2026-09-10 | Answered the open portability/trust questions. Recorded the operator direction that logic which cannot be proven safe for the shared PQS path should be explicitly classified and, where support is justified, executed only through an isolation/sandbox boundary. | research-worker | Operator follow-up in ChatGPT; StarLang logic protocol/IR/SWI adapter and PQS safe-boundary source reviewed 2026-09-10 |
| 2026-09-10 | Recorded the operator requirement that reusable star-kb rules must be consumable by direct Prolog actors and StarLang logic actors as well as prolog-query-server. | research-worker | Operator follow-up in ChatGPT; repository source reviewed 2026-09-10 |

* Research Question

Can =lost-rob0t/star-kb= serve as one reusable source of facts and rules for multiple execution styles rather than being coupled to =prolog-query-server=, specifically allowing direct Prolog actors and StarLang logic actors to reuse the same rule knowledge while preserving a strong trust boundary for logic that requires more power than PQS's current safe subset?

This research is now approved. Its implementation decisions are carried forward into [[id:star-kb-design-002-portable-logic-packages-sandboxed-actors][STAR-KB-002]]. Approval of this research does not approve that design or authorize implementation.

* Operator Requirements

- =star-kb= is the authority for reusable logical packages; PQS is one consumer/runtime, not the sole semantic owner.
- The same reviewed rules should be reusable by PQS-backed actors, direct/native Prolog actors, and StarLang logic actors where their declared semantic profile supports them.
- Logic that cannot be admitted to the safe shared-service subset should be visibly classified rather than silently accepted as ordinary safe logic.
- A sandbox/isolation path is preferred for approved logic that requires native or otherwise higher-risk Prolog capabilities.

#+begin_src text
                     star-kb package
                           |
             +-------------+-------------+
             |             |             |
             v             v             v
           PQS         native Prolog   StarLang logic
      safe interpreter     actor           actor
             |             |               |
             |      sandbox when needed    |
             +-------------+---------------+
                  same package semantics
#+end_src

* Verified Existing Runtime Evidence

** StarLang

Current =star-logic-protocol= already defines backend descriptors with semantic profiles, capabilities, isolation classes, hard limits, cooperative limits, build identity, and version. It defines =open-logic-session= with =package-id= and =package-digest= and generic fact-delta, operation, result, cancellation, status, and health boundaries.

Current =star-logic-ir= separates immutable logical package identity from backend materialization identity. It records =package-id=, =package-version=, =package-digest=, =mapping-id=, and =mapping-digest= and carries a semantic profile, backend policy, required capabilities, hard limits, required isolation, bindings, proof/answer policy, and budget. This is direct evidence that safety/isolation requirements can be represented independently from the package identity.

The current =star-logic-adapter-swi= provides a trusted process boundary, exact executable/build identity, authenticated MQI framing, a repository-owned bootstrap, process-isolated sessions, health, and orderly reaping. It deliberately does not expose arbitrary caller-controlled =consult/1=, raw goals, modules, files, or predicate indicators. Portable query/fact operations remain incomplete.

** prolog-query-server

PQS persists a restricted JSON AST, validates stored facts/rules, executes them through a bounded meta-interpreter, and does not expose arbitrary Prolog =call/1=. Its safe builtin registry is intentionally small: unification/disequality, bounded numeric calculation/comparison, and shape/type tests. This is already a strong safe-by-construction execution tier.

The evidence therefore does not support weakening the existing shared PQS interpreter merely to gain native Prolog power. A stronger capability should be a separate execution class with a separate isolation boundary.

* Research Finding: Safety and Portability Are Separate Axes

The operator's proposed "unsafe" label is useful, but one label should not collapse two different questions:

#+begin_src text
PORTABILITY                         EXECUTION TRUST
-----------                         ---------------
portable/core                       safe-by-construction
backend extension                   sandbox-required / privileged
native-only                         unsupported / prohibited in a given route
#+end_src

A rule may be backend-specific but harmless, such as a backend-specific tabling feature. A different rule may be syntactically common Prolog but dangerous in a shared service, such as dynamic code loading, file I/O, network access, shell execution, foreign calls, or unrestricted meta-calls.

The approved research direction therefore preserves two independent declarations: what semantic subset the package needs, and what execution isolation/trust class that subset requires.

* Research Finding: Fail Closed, Do Not Trust Self-Declared Safety

A package author may declare intent, but the runtime/compiler must verify whether a package actually fits the claimed safe subset. An arbitrary package must not become trusted merely because metadata says =safe=.

The approved trust behavior is:

- a package proven to use only the portable safe subset may execute through the normal PQS/shared-safe path;
- a package using an approved higher-risk capability is marked as requiring isolation and may execute only through an eligible sandboxed backend;
- a package using an unsupported or prohibited capability is rejected for that execution route;
- unclassified constructs fail closed rather than being treated as safe by default.

This is especially important for =consult/1= and related loading behavior: run-time input must never become arbitrary executable source in the trusted PQS process.

* Research Finding: Sandbox the Native-Power Path, Not the Canonical Knowledge

PQS's current restricted interpreter is already a logical sandbox in the sense that stored knowledge cannot reach arbitrary native Prolog operations. If later actors need approved native Prolog extensions, the approved direction is an external worker/process sandbox rather than consulting such rules directly into the shared PQS process.

A useful isolation boundary should enforce, at minimum, no ambient credentials, no network unless explicitly granted, a read-only or narrowly scoped filesystem, CPU/time/memory/process limits, deterministic worker teardown/reaping, explicit executable/build identity, and capability-scoped access to any external effect.

A sandbox does not make arbitrary logic trustworthy. It reduces blast radius. Static validation, package provenance, digest pinning, capability admission, and runtime limits still matter.

* Answers to the Previously Open Questions

** 1. Smallest useful semantic profile shared by PQS and direct SWI-Prolog

*Answer:* a pure, bounded Horn-style rule subset with ground facts; variables in rule/query terms; conjunction; unification and disequality; structured terms/lists; deterministic numeric calculation and comparisons; and bounded recursion/inference. For weighted/salient rules, the profile must also have a deterministic way to combine numeric evidence.

The portable core excludes arbitrary meta-call, dynamic source loading, I/O, shell/network access, foreign predicates, uncontrolled global mutation, and engine-specific control effects. Those features are not required for the weighted-salience dialect baseline.

** 2. Which constructs are portable core versus extensions?

*Answer:* classify by semantics, not by file extension or engine name.

Portable/core candidates are declarative facts and rules, ordinary terms, conjunction, bounded recursion, unification/disequality, deterministic safe arithmetic/comparison, and named query operations whose answer semantics are defined independently of one engine.

Backend extensions include features whose semantics are legitimate but not uniformly available, such as engine-specific tabling, constraint systems, indexing hints, specialized aggregates, or backend-native proof facilities. These require capability/profile negotiation and parity tests.

Sandbox-required/native-power constructs include dynamic consult/load, unrestricted meta-call, filesystem or network I/O, process/shell execution, foreign/native calls, mutable global code/state, and similar effects. A backend may refuse them entirely even when another backend permits them in a sandbox.

** 3. Mapping one star-kb package to StarLang package/mapping identity

*Answer:* =package-id/package-version/package-digest= identifies the canonical reviewed logical package and its semantics. =mapping-id/mapping-digest= identifies the backend-specific materialization/translation used to load that package into PQS, SWI, or another backend. Backend build identity and required isolation are separate runtime evidence.

Therefore two actors can prove they used the same rules even if one used a PQS mapping and another used an SWI mapping. A changed backend translator changes the mapping digest without pretending that the canonical package changed.

** 4. Conformance fixture format for semantic parity

*Answer:* use backend-neutral competency fixtures tied to the package digest. Each fixture identifies the named operation/query, input bindings/fact delta, expected normalized answer set or multiset, expected success/failure/abstention behavior, and proof invariants when proofs are part of the profile. Ordering is ignored unless ordering is explicitly part of the semantic contract.

Negative fixtures cover unsupported capability, unsafe construct, malformed delta, resource exhaustion, and attempted package mutation. Backend-specific proof formatting may differ; parity compares normalized semantic claims rather than byte-identical traces.

** 5. Trust boundary for direct Prolog actors

*Answer:* direct actors never =consult/1= arbitrary run-time/user input. They load only digest-pinned, validated =star-kb= package materializations through a trusted loader. Safe packages may run in the normal isolated logic worker. Packages requiring native-power capabilities require a sandbox/isolation class and an explicit capability grant.

The sandbox path fails closed when its required controls are unavailable. A package that needs prohibited capabilities does not run merely because the actor is local.

** 6. Which facts may be supplied as per-actor/session deltas?

*Answer:* actor/session deltas may contain ephemeral data, not new executable semantics. Good candidates are ground observations, actor-private facts, document features, model outputs, weights learned for that run, query context, and temporary entity relationships whose predicate vocabulary is declared by the package.

A delta must not replace canonical rules, define new predicates/code, override reserved predicates, change package metadata, or silently persist into the reusable package. The package digest remains unchanged; the session/run records a separate input/evidence digest or provenance identity.

** 7. When should an actor use local execution versus PQS?

*Answer:* prefer PQS when centralized release selection, shared durable knowledge, consistent cross-actor state, server-side authorization, audit/proof provenance, or one authoritative runtime release is valuable. Prefer local/direct execution when latency, high per-actor throughput, offline operation, actor-private ephemeral evidence, or an approved backend-specific capability materially benefits the workload.

Higher-risk native Prolog logic is not a reason to weaken the shared PQS process. It uses a sandboxed native worker/local backend or a future explicitly sandboxed PQS worker class. Backend choice preserves the same package semantics and provenance.

** 8. Should LISA and N-Prolog be initial parity targets?

*Answer:* no. The first portability proof targets PQS and native SWI-Prolog because both have concrete current runtime evidence and the SWI adapter foundation already exists. LISA and N-Prolog remain later compatibility targets behind the same semantic-profile/capability model.

* Implications for Weighted/Salient Dialect Rules

The weighted-salience findings in STAR-KB-RESEARCH-003 remain unchanged. The portable package should own reusable associations, salience semantics, reliability/exposure rules, redundancy handling, and named logical operations. Actor/session observations remain data inputs.

The safe portable subset is sufficient for the intended first dialect baseline: it does not need filesystem access, shell execution, arbitrary dynamic consult, foreign code, or network side effects. This makes dialect a good parity case between PQS and native SWI execution.

* Research Conclusion

This research conclusion is *APPROVED* by the StarIntel operator.

=star-kb= is the authority for reusable logical packages independently from any one execution backend. PQS remains the preferred shared safe execution path for the portable bounded subset. Direct Prolog and StarLang logic actors may materialize the same package locally when the selected backend satisfies the package's semantic profile, capability, hard-limit, and isolation requirements.

Portability class and execution trust class are separate. Logic that cannot be proven to fit the shared safe subset does not enter the trusted PQS process. Approved native-power constructs may execute only in an explicitly eligible sandboxed worker; unsupported constructs remain rejected.

The next lifecycle node is [[id:star-kb-design-002-portable-logic-packages-sandboxed-actors][STAR-KB-002 Portable Logic Packages and Sandboxed Actors]]. That design is =PENDING= operator review. No implementation authorization is implied by this research approval.

* Sources

** StarIntel repositories reviewed 2026-09-10

- [[https://github.com/lost-rob0t/star-lang/blob/main/star-logic-protocol/src/protocol.lisp][StarLang star-logic-protocol backend/session protocol]]
- [[https://github.com/lost-rob0t/star-lang/blob/main/star-logic-ir/src/logic-ir.lisp][StarLang star-logic-ir package identity, mappings, required capabilities, limits, and isolation]]
- [[https://github.com/lost-rob0t/star-lang/blob/main/star-logic-adapter-swi/README.md][StarLang SWI-Prolog adapter current trust/process boundary]]
- [[https://github.com/lost-rob0t/prolog-query-server/blob/main/docs/builtins.md][PQS safe expert-system builtin surface]]
- [[https://github.com/lost-rob0t/prolog-query-server/blob/main/src/kb_service.pl][PQS KB query/release service]]
- [[https://github.com/lost-rob0t/prolog-query-server][prolog-query-server]]
- [[https://github.com/lost-rob0t/star-kb][star-kb]]
- [[id:star-kb-research-003-dialect-enrichment-corpus-inference-evidence][STAR-KB-RESEARCH-003 Dialect Enrichment Corpus and Inference Evidence]]

* Related Nodes

- [[id:star-kb-design-002-portable-logic-packages-sandboxed-actors][STAR-KB-002 Portable Logic Packages and Sandboxed Actors]]
- [[id:star-kb-index-000-prolog-knowledge-system][STAR-KB-INDEX-000 Prolog Knowledge System]]

* Footnotes and Glossary

- *Logic package* :: A versioned/digest-identified reusable unit of logical facts, rules, named operations, and declared semantic requirements.
- *Materialization* :: Translating/loading one canonical logic package into the representation required by a concrete execution backend without changing its intended semantics.
- *Semantic profile* :: A named behavioral contract a logic backend declares it can execute; StarLang uses this during backend compatibility/selection.
- *Portability class* :: Whether logic belongs to the common portable subset or depends on a backend extension/native semantics.
- *Execution trust class* :: Whether the logic may execute in the normal safe path, requires an isolation/sandbox boundary and explicit capabilities, or is unsupported/prohibited for that route.
- *Sandbox-required* :: Logic that is not admitted to the shared safe subset and, if supported at all, must execute through an isolated worker with bounded capabilities/resources.
- *Backend mapping* :: Versioned logic/materialization code that maps a portable package/operation contract onto a concrete backend representation or operation surface.
- *PQS* :: =prolog-query-server=, the existing CouchDB-backed bounded expert-system service.
- *Direct Prolog actor* :: An actor whose logic executes against a local/native Prolog backend instead of requiring an HTTP query to PQS.
- *StarLang logic actor* :: A StarLang actor using the StarLang logic protocol/IR to execute a digest-identified logic package through an eligible backend.
- *Session delta* :: Ephemeral, schema-constrained run data supplied to a logic session without changing the canonical package identity.
- *Semantic parity* :: Evidence that the same logical package produces equivalent defined behavior across claimed compatible backend materializations.