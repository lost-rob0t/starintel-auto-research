#+title: Starintel Auto Research

* Purpose

This repository is the Emacs-first control plane for Starintel research, design files, SRFCs, implementation promotion, and graph-oriented IDE surfaces.

The Org tree under =roam/= is the source of truth. Elisp under =lisp/= owns capture, indexing, validation, context bundling, Git workflow helpers, Org-roam database operations, and GitHub Pages export.

Published second brain: [[https://lost-rob0t.github.io/starintel-auto-research/][lost-rob0t.github.io/starintel-auto-research]]

* Repository Shape

#+begin_example
lisp/starintel/                Emacs workflow and publishing implementation
roam/design/                   Numbered design files
roam/research/                 Research trackers and findings
roam/implement/                Designs promoted for implementation
roam/indexes/                  Roadmaps and project indexes
pages/                         Batch bootstrap and static web assets
.github/workflows/pages.yml    GitHub Pages build and deployment
ui/web/                        Web graph IDE
ui/desktop/                    Desktop graph IDE
#+end_example

* Org-roam Second Brain

Load =lisp/starintel/second-brain.el= from Emacs, then use:

#+begin_example
M-x star/roam
M-x star/roam-capture
M-x star/roam-sync
M-x star/pages-build
M-x star/pages-open
#+end_example

The repository =.dir-locals.el= points Org-roam at =roam/= and stores its generated database under =.cache/=.

Enable GitHub Pages workflow publishing once with an administrator-authenticated GitHub CLI session:

#+begin_src sh
bash scripts/configure-pages
#+end_src

The ordinary workflow token cannot create or change the repository's initial Pages setting. The command configures =build_type=workflow= through the GitHub Pages API and triggers the Pages workflow on =main=.

Build the same site used by GitHub Pages:

#+begin_src sh
bash scripts/publish-pages
python3 scripts/check-pages-links.py _site
#+end_src

The export uses a temporary normalized copy of the Org tree, builds the Org-roam database, exports one HTML page per Org file, adds backlinks, creates search and graph indexes, and checks every internal page, asset, and anchor link.

See [[file:roam/indexes/second-brain/SECOND-BRAIN-000-org-roam-pages.org][SECOND-BRAIN-000 Org-roam Pages]].

* Rule

Do not load the entire research tree into an agent context. Build a bounded context bundle from the active node, its direct links, and its project index.
