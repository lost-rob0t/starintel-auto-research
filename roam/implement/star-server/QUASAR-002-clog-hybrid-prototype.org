:PROPERTIES:
:ID:       quasar-clog-hybrid-prototype-002-implementation
:END:
#+title: QUASAR-002 CLOG Hybrid Prototype Architecture — Active Implementation
#+description: Track the bounded implementation of the first Quasar Common Lisp, CLOG, and Cytoscape prototype slice.
#+status: IMPLEMENTING
#+filetags: :starintel:quasar:implementation:common-lisp:clog:cytoscape:
#+created: [2026-07-23 Thu]
#+last_modified: [2026-07-23 Thu]

* Canonical Design

- [[file:../../design/star-server/QUASAR-002-clog-hybrid-prototype.org][QUASAR-002 CLOG Hybrid Prototype Architecture]]
- [[file:../../indexes/star-server/STAR-INDEX-000-fourth-generation.org][STAR-INDEX-000 StarIntel Fourth Generation]]

* Active Slice

This implementation is limited to the first prototype checkpoint:

1. loadable =quasar/core= and =quasar/ui-clog= systems;
2. a renderer-independent Common Lisp graph model;
3. a CLOG[fn:clog] workbench shell;
4. one Cytoscape.js[fn:cytoscape] composite graph component;
5. one browser-to-Lisp =rendererReady= signal;
6. projection of two nodes and one edge from the read-evaluate-print loop[fn:repl];
7. dependency-light core tests.

Editing gestures, action execution, persistence, Star-Lang integration, remote services, and the public Quasar protocol remain outside this checkpoint.

* Files

- =prototypes/quasar/common-lisp/quasar.asd=
- =prototypes/quasar/common-lisp/packages.lisp=
- =prototypes/quasar/common-lisp/core/graph-model.lisp=
- =prototypes/quasar/common-lisp/core/cytoscape-json.lisp=
- =prototypes/quasar/common-lisp/ui/clog-application.lisp=
- =prototypes/quasar/common-lisp/static/quasar-cytoscape.js=
- =prototypes/quasar/common-lisp/static/quasar.css=
- =prototypes/quasar/common-lisp/tests/core-tests.lisp=
- =prototypes/quasar/common-lisp/README.md=

* Validation Status

Static validation performed in the research environment:

- Common Lisp delimiter and string-balance scan;
- JavaScript syntax check with =node --check=;
- mocked JavaScript bridge check covering dynamic Cytoscape loading, =rendererReady=, and graph replacement;
- dependency-boundary check confirming =quasar/core= does not depend on CLOG;
- branch diff review.

The research environment does not contain SBCL[fn:sbcl] or a CLOG installation and cannot reach GitHub from its command-line container. Runtime loading, browser startup, and the real WebSocket callback therefore remain unverified until run in the target development environment.

* Stop Condition

Stop after the graph is visibly projected and the =rendererReady= callback is observed. Review the boundary before implementing graph editing.

* Footnotes and Glossary

[fn:clog] CLOG: Common Lisp Omnificent GUI, a Common Lisp framework that uses a browser for rendering and WebSockets for continued communication with Lisp.

[fn:cytoscape] Cytoscape.js: A JavaScript library that renders and analyzes node-and-edge graphs in a web browser.

[fn:repl] Read-evaluate-print loop: An interactive programming prompt that reads an expression, evaluates it, prints the result, and repeats.

[fn:sbcl] SBCL: Steel Bank Common Lisp, a widely used compiler and runtime for the Common Lisp programming language.
